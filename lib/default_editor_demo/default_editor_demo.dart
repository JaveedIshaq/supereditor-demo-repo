import 'package:flutter/material.dart';

class DefaultEditorDemo extends StatelessWidget {
  const DefaultEditorDemo({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Deualt Style Demo"),
      ),
    );
  }
}
